mov 3, db
msg db 5
aux 5, 3
// memory allocation for AUX
// 
/*
@Params 
Badcode.exe
*&/
msg db "E"
string e
  connect "E"

public unsignd integer UID;
aux 3, 7;
// boot from DISK since os requires too much time

get * from using db * Disk /linux/theb/

if (not &*) return This.objectTypes.nullifier

This.Prototypes["OS"] = {
  ["Out", "In"][1] = [] //  
  ["Boot"] = [
    This.Prototypes["Native"].GetAttribute("GarbageCollection"):Initiate(); 
  ],

}